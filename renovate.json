{
  "extends": ["config:base"],
  "packageRules": [
    {
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["major", "minor", "patch"],
      "matchManagers": ["dockerfile"],
      "dependencyDashboard": true
    },
    {
      "matchManagers": ["pip_requirements"],
      "matchPackagePatterns": ["*"],
      "dependencyDashboard": true
    }
  ],
  "regexManagers": [
    {
      "fileMatch": ["^monitoring-stack\\.yaml$", "^monitoring-traefik\\.yaml$"],
      "matchStrings": ["image:\\s?(?<depName>[\\w\\-/]+):(?<currentValue>[\\w\\-\\.]+)"],
      "datasourceTemplate": "docker"
    }
  ],
  "prHourlyLimit": 5, // Limit the number of Pull Requests created to 5 per hour
  "rateLimitPerHour": 100 // Limit the number of API calls to 50 per hour
}
